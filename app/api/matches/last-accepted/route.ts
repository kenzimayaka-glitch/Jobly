import {NextRequest,NextResponse} from "next/server";import {authUser,adminClient,ensureUser} from "../../../../lib/mobilityServer";
export async function GET(req:NextRequest){try{const au=await authUser(req);if(!au)return NextResponse.json({message:"Session requise."},{status:401});const sb=adminClient(),u=await ensureUser(sb,au);const {data,error}=await sb.from("Application").select("id,jobId,recruiterJobId,Job:jobId(id,location,companyId),RecruiterJob:recruiterJobId(id,location,companyName)").eq("userId",u.id).in("status",["OFFER","INTERVIEW"]).order("updatedAt",{ascending:false}).limit(1).maybeSingle();if(error)throw new Error(error.message);return NextResponse.json({application:data||null})}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Aucune correspondance."},{status:500})}}
