import {NextRequest,NextResponse} from "next/server";import {authUser,adminClient,ensureUser} from "../../../../../lib/mobilityServer";
export async function GET(req:NextRequest){try{const au=await authUser(req);if(!au)return NextResponse.json({message:"Session requise."},{status:401});const sb=adminClient(),u=await ensureUser(sb,au),{data,error}=await sb.from("PartnerExtended").select("*").eq("partnerUserId",u.id).maybeSingle();if(error)throw new Error(error.message);return NextResponse.json({partner:data||{type:"FINANCE",code:null}})}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Profil indisponible."},{status:500})}}
