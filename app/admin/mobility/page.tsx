"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import PageHeader from "@/components/PageHeader";import {getSupabaseClient} from "@/lib/supabase";import MobilityKanban from "@/components/mobility/MobilityKanban";
export default function AdminMobility(){const router=useRouter();const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const s=await getSupabaseClient().auth.getSession();const t=s.data.session?.access_token;if(!t)return;const r=await fetch("/api/mobility/partner/reports",{headers:{Authorization:`Bearer ${t}`}});if(r.ok){const text=await r.text();setRows(text.split("\n").slice(1).filter(Boolean).map(x=>{const a=x.split(";");return{id:a[0],label:`${a[1]} → ${a[2]}`,status:a[6]}}))}})()},[]);return <main className="min-h-[100dvh] bg-[#F7FAFF] pb-8"><PageHeader eyebrow="ADMIN" label="Mobility" onBack={()=>router.push("/admin")}/><div className="px-5 py-8"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-black">Mobility Admin</h1><p className="mt-2 text-sm text-slate-500">Kanban opérationnel des dossiers.</p><div className="mt-6"><MobilityKanban initial={rows}/></div></div></div></main>}
