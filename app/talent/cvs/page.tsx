"use client";
import { ChangeEvent, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import PageHeader from "../../../components/PageHeader";
import BottomNav,{TALENT_NAV} from "../../../components/BottomNav";
import TalentBackground from "../../../components/TalentBackground";

type CV={id:string;name:string;source:string;fullName:string;headline:string;email:string;phone:string;summary:string;skills:string;experience:string;education:string;ats:number;createdAt:string};
const KEY="jobly:talent-cvs";
const empty:CV={id:"",name:"Mon CV Jobly",source:"Créé dans Jobly",fullName:"",headline:"",email:"",phone:"",summary:"",skills:"",experience:"",education:"",ats:0,createdAt:""};
function score(c:CV){let n=0; if(c.fullName.trim())n+=15;if(c.headline.trim())n+=10;if(c.email.trim())n+=10;if(c.phone.trim())n+=5;if(c.summary.trim().length>=80)n+=15;else if(c.summary.trim())n+=7;if(c.skills.split(',').filter(Boolean).length>=5)n+=20;else if(c.skills.trim())n+=10;if(c.experience.trim().length>=120)n+=15;else if(c.experience.trim())n+=7;if(c.education.trim())n+=10;return Math.min(100,n)}
export default function TalentCVs(){const router=useRouter();const [cvs,setCvs]=useState<CV[]>([]);const [cv,setCv]=useState<CV>({...empty,id:crypto.randomUUID(),createdAt:new Date().toISOString()});const [file,setFile]=useState("");const [message,setMessage]=useState("");useEffect(()=>{try{setCvs(JSON.parse(localStorage.getItem(KEY)||"[]"))}catch{}},[]);const ats=useMemo(()=>score(cv),[cv]);function update(k:keyof CV,v:string){setCv(x=>({...x,[k]:v}))}function save(){const next={...cv,ats,createdAt:cv.createdAt||new Date().toISOString(),name:cv.name||"Mon CV Jobly"};const rest=cvs.filter(x=>x.id!==next.id);const all=[next,...rest];setCvs(all);localStorage.setItem(KEY,JSON.stringify(all));setCv(next);setMessage("CV enregistré.")}function importFile(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;setFile(f.name);const reader=new FileReader();reader.onload=()=>{const text=typeof reader.result==='string'?reader.result:'';if(f.type==='text/plain')setCv(x=>({...x,summary:x.summary||text.slice(0,1000),source:`Importé : ${f.name}`}));else setCv(x=>({...x,source:`Importé : ${f.name}`}));setMessage(`${f.name} chargé. Pour PDF/DOCX, complète ou vérifie les champs avant de générer la version ATS.`)};reader.readAsText(f)}function print(){window.print()}function load(x:CV){setCv(x);setMessage("Version chargée.")}return <main className="talent-shell relative min-h-[100dvh] bg-[#F7FAFF] pb-28 text-navy"><TalentBackground/><div className="relative z-10 print:bg-white"><PageHeader label="Mes CV" eyebrow="TALENT" initial="T" onBack={()=>router.push("/talent/profile")} theme="talent"/><div className="mx-auto max-w-4xl space-y-4 px-5 py-5"><section className="rounded-[24px] bg-white p-5 shadow-sm print:hidden"><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-xl font-black">CV Builder + ATS</h1><p className="mt-1 text-xs text-jobly-gray">Importe un CV ou crée une version Jobly prête à imprimer.</p></div><label className="cursor-pointer rounded-full bg-[#FFC72C] px-4 py-3 text-xs font-black">Charger PDF/DOCX<input type="file" accept=".pdf,.doc,.docx,.txt" onChange={importFile} className="hidden"/></label></div>{file&&<p className="mt-3 text-xs font-bold text-jobly-blue">Fichier : {file}</p>}</section><div className="grid gap-4 lg:grid-cols-[1.4fr_.6fr]"><section className="rounded-[24px] bg-white p-5 shadow-sm"><div className="grid gap-3 sm:grid-cols-2"><input value={cv.fullName} onChange={e=>update('fullName',e.target.value)} placeholder="Nom complet" className="rounded-xl border px-4 py-3"/><input value={cv.headline} onChange={e=>update('headline',e.target.value)} placeholder="Titre professionnel" className="rounded-xl border px-4 py-3"/><input value={cv.email} onChange={e=>update('email',e.target.value)} placeholder="Email" className="rounded-xl border px-4 py-3"/><input value={cv.phone} onChange={e=>update('phone',e.target.value)} placeholder="Téléphone" className="rounded-xl border px-4 py-3"/></div><textarea value={cv.summary} onChange={e=>update('summary',e.target.value)} placeholder="Résumé professionnel" rows={4} className="mt-3 w-full rounded-xl border px-4 py-3"/><textarea value={cv.skills} onChange={e=>update('skills',e.target.value)} placeholder="Compétences (séparées par des virgules)" rows={3} className="mt-3 w-full rounded-xl border px-4 py-3"/><textarea value={cv.experience} onChange={e=>update('experience',e.target.value)} placeholder="Expériences professionnelles" rows={6} className="mt-3 w-full rounded-xl border px-4 py-3"/><textarea value={cv.education} onChange={e=>update('education',e.target.value)} placeholder="Formation / certifications" rows={4} className="mt-3 w-full rounded-xl border px-4 py-3"/><div className="mt-4 grid gap-2 sm:grid-cols-3 print:hidden"><button onClick={save} className="rounded-xl bg-jobly-blue py-3 font-black text-white">Enregistrer</button><button onClick={print} className="rounded-xl bg-[#FFC72C] py-3 font-black">Imprimer / PDF</button><button onClick={()=>router.push('/career-os')} className="rounded-xl border py-3 font-black">Career OS →</button></div>{message&&<p className="mt-3 text-xs font-bold text-jobly-blue print:hidden">{message}</p>}</section><aside className="space-y-4 print:hidden"><section className="rounded-[24px] bg-white p-5 shadow-sm"><span className="text-xs font-black uppercase text-jobly-gray">Score ATS</span><div className="mt-2 text-5xl font-black text-jobly-blue">{ats}%</div><p className="mt-2 text-xs text-jobly-gray">Score déterministe basé sur la complétude et la présence de sections lisibles par ATS.</p><ul className="mt-4 space-y-2 text-xs font-semibold"><li>{cv.fullName?'✓':'○'} Identité</li><li>{cv.summary.length>=80?'✓':'○'} Résumé structuré</li><li>{cv.skills.split(',').filter(Boolean).length>=5?'✓':'○'} 5+ compétences</li><li>{cv.experience.length>=120?'✓':'○'} Expérience détaillée</li><li>{cv.education?'✓':'○'} Formation</li></ul></section><section className="rounded-[24px] bg-white p-5 shadow-sm"><h2 className="font-black">Mes versions</h2>{cvs.length===0?<p className="mt-2 text-xs text-jobly-gray">Aucune version enregistrée.</p>:<div className="mt-3 space-y-2">{cvs.slice(0,8).map(x=><button key={x.id} onClick={()=>load(x)} className="w-full rounded-xl border p-3 text-left"><b className="block text-xs">{x.name}</b><span className="text-[10px] text-jobly-gray">ATS {x.ats}% · {x.source}</span></button>)}</div>}</section></aside></div><section id="cv-print" className="mx-auto max-w-3xl rounded-[8px] bg-white p-8 shadow-sm print:mt-0 print:p-0 print:shadow-none"><h2 className="text-3xl font-black">{cv.fullName||'Nom complet'}</h2><p className="mt-1 text-lg font-bold text-jobly-blue">{cv.headline||'Titre professionnel'}</p><p className="mt-2 text-xs">{[cv.email,cv.phone].filter(Boolean).join(' · ')}</p>{[['Profil',cv.summary],['Compétences',cv.skills],['Expérience',cv.experience],['Formation',cv.education]].map(([h,v])=>v?<div key={h} className="mt-5"><h3 className="border-b pb-1 text-sm font-black uppercase">{h}</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-6">{v}</p></div>:null)}</section></div></div><BottomNav active="/dashboard" items={TALENT_NAV}/></main>}
