"use client";
import {useState} from "react";import {getSupabaseClient} from "@/lib/supabase";
export default function Validations(){const [code,setCode]=useState(""),[message,setMessage]=useState("");async function validate(){const s=await getSupabaseClient().auth.getSession();const t=s.data.session?.access_token;if(!t){setMessage('Session requise.');return}const r=await fetch('/api/mobility/admin/generate-pass',{method:'POST',headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:JSON.stringify({requestId:code})});const b=await r.json();setMessage(r.ok?`Pass généré : ${b.passCode}`:b.message)}return <main className="min-h-[100dvh] bg-white px-5 py-8"><div className="mx-auto max-w-lg"><h1 className="text-3xl font-black">Valider un Pass</h1><p className="mt-2 text-sm text-slate-500">Entrez l'identifiant de demande autorisé par Jobly.</p><input value={code} onChange={e=>setCode(e.target.value)} placeholder="ID de demande" className="mt-6 w-full rounded-2xl border p-3"/><button onClick={validate} className="mt-4 w-full rounded-full bg-[#0A3D9C] p-3 font-black text-white">Valider</button>{message&&<p className="mt-4 rounded-2xl bg-slate-50 p-4 text-sm font-bold">{message}</p>}</div></main>}
